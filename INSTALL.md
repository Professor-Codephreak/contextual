git clone https://github.com/Professor-Codephreak/contextual/<br />
cd contextual<br />
python3 -m venv contextual<br />
source contextual/bin/activate  # On Windows, use `venv\Scripts\activate`<br />
pip install -r requirements.txt<br />
